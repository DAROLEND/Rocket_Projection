ANTHROPIC_API_KEY=sk-ant-...

# Postgres — optional. Leave unset for local dev (falls back to a local
# rocket.db SQLite file, no setup needed). Set these to point at a free
# Postgres (Supabase, Neon, ...) in production.
# DB_HOST=db.xxxxxxxxxxxx.supabase.co
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASS=
